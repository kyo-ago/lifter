{
  "name": "@lifter/lifter-main",
  "description": "lifter proxy main process",
  "version": "0.0.13",
  "author": "@kyo_ago",
  "dependencies": {
    "@lifter/electron-window-manager": "^0.0.13",
    "@lifter/lifter-common": "^0.0.13",
    "@lifter/networksetup-proxy": "^0.0.13",
    "http-mitm-proxy": "^0.6.1",
    "ifconfig": "^0.0.1",
    "mime": "^2.2.0",
    "nedb": "^1.8.0"
  },
  "devDependencies": {
    "@types/mime": "^2.0.0",
    "@types/nedb": "^1.8.5"
  },
  "directories": {
    "test": "src/"
  },
  "license": "GPL-3.0",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc -p . --rootDir ./src/",
    "clean": "rm -fr package-lock.json yarn.lock node_modules build",
    "depcruise": "depcruise --validate .dependency-cruiser.json src",
    "test": "NODE_ENV='test' mocha --require ts-node/register tests/mocks/require-mocks/*.ts tests/mocks/require-mocks/**/*.ts {src,tests}/**/*.spec.ts"
  },
  "types": "./build/index.d.ts"
}
