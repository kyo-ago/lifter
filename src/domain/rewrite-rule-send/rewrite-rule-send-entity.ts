import {BaseEntity} from "../base/base-entity";
import {RewriteRuleSendIdentity} from "./rewrite-rule-send-identity";

export class RewriteRuleSendEntity extends BaseEntity<RewriteRuleSendIdentity> {
}